{
  "name": "@xsec/llm-redteam",
  "version": "0.14.0",
  "private": true,
  "description": "Offensive LLM/agent red-team engine: generate indirect-prompt-injection & jailbreak payloads from a sourced strategy library, drive a target adapter, judge breaks, and iterate. The attack-side mirror of packages/benchmark's detection suite.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "xsec-llm": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@xsec/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
