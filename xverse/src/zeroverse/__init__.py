"""0verse — automated reverse engineering for vulnerability discovery.

The importable package is ``zeroverse`` (Python identifiers cannot begin with a
digit); the brand and CLI command are ``0verse``.
"""

__version__ = "0.0.1"
