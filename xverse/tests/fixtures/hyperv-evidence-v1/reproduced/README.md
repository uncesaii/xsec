# Sanitized Hyper-V evidence contract fixture

This directory is a non-executing producer/consumer contract fixture. The two
`.dmp` files are inert text placeholders, not Windows crash dumps. The fixture
contains no trigger binary, exploit payload, private dump, or real vulnerability
evidence. It is never claim-eligible and must not be submitted or counted as a
discovered vulnerability.
